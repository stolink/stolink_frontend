interface TreeLinesProps {
  level: number;
  isLast: boolean;
  parentLines: boolean[];
}

/**
 * 트리 구조의 계층을 시각화하는 연결선 컴포넌트
 *
 * 미니멀 디자인을 위해 연결선 제거 - 들여쓰기만으로 계층 표현
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function TreeLines(_props: TreeLinesProps) {
  return null;
}
